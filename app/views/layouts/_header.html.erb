<header class="navbar navbar-fixed-top navbar-inverse header-color">
  <div class="container">
    <nav class='nav navbar-nav navbar-left' >
      <% if user_signed_in? %>
        <span><%= link_to 'Bookers', user_path(current_user) %></span>
      <% else %>
        <span><%= link_to 'Bookers', root_path %></span>
      <% end %>
    </nav>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to ' ユーザー', users_path, class: 'glyphicon glyphicon-user users' %></li>
          <li><%= link_to "通知", notifications_path %></li>
          <li><%= link_to ' 投稿', books_path, class: 'glyphicon glyphicon-book books'%></li>
          <li><%= link_to ' ログアウト', destroy_user_session_path, method: :delete, class: 'glyphicon glyphicon-log-out logout' %></li>
        <% else %>
          <li><%= link_to 'Bookersとは', about_path %></li>
          <li><%= link_to '新規会員登録', new_user_registration_path, class: 'btn registration-btn' %></li>
          <li><%= link_to 'ログイン', new_user_session_path, class: '"btn btn-light header-btn"'%></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>